import React from 'react'; const Alert = ({ message, type }) => <div className={`p-3 rounded mb-4 ${type === 'error' ? 'bg-red-100 text-red-700' : 'bg-green-100 text-green-700'}`}>{message}</div>; export default Alert;
